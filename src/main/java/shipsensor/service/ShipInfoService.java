package shipsensor.service;

import com.baomidou.mybatisplus.extension.service.IService;
import shipsensor.entity.ShipInfo;

/**
 * 船舶基础信息 Service 接口
 */
public interface ShipInfoService extends IService<ShipInfo> {
    // 这里可以定义自定义的业务方法，例如：
    // boolean checkImoExists(String imo);
}